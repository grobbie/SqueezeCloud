soundcloud plugin for squeezebox server, based off YouTube plugin

Now listed in the semi-official 3rd party repo.

add it via settings -> plugins -> Show all 3rd party plugins -> Soundcloud

TODO:
-slowwwwwwww, need to cache api responses?
-issues with how long it takes to load metadata

